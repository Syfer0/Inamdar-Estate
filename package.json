{
  "name": "inamdarconstruction",
  "version": "1.0.0",
  "scripts": {
    "build": "npm install && npm install --prefix client && npm run build --prefix client && npm run build:server",
    "build:server": "cd server && npm install && npm run build:server"
  },
  "dependencies": {},
  "devDependencies": {}
}
