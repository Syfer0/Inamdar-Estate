{
  "name": "inamdarconstruction",
  "version": "1.0.0",
  "scripts": {
    "dev": " cd server && nodemon api/index.js",
    "start": "node api/index.js",
    "build": " cd client  && npm install --prefix client && npm run build --prefix client ",
    "build:server": "cd server && npm install && npm run build:server"
  },
  "dependencies": {},
  "devDependencies": {}
}
